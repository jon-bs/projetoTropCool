import { ConfiguracaoAgenda } from './configuracao-agenda';

describe('ConfiguracaoAgenda', () => {
  it('should create an instance', () => {
    expect(new ConfiguracaoAgenda()).toBeTruthy();
  });
});
